# Polymarket Bot

This repo now contains two Playwright workflows:

- `polymarket_watcher/` – headful watcher that records Won/Lost outcomes per Bitcoin 15-minute event into `results.txt`.
- `polymarket_bot_py/` – original trade-filling bot (see its README for details).
